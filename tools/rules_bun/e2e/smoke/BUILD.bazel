"""소비자 관점 검증.

룰셋 내부 테스트는 `//bun/tests` 에서 돈다. 여기서는 **외부 모듈이 공개 API
만으로 툴체인을 쓸 수 있는가**만 확인한다. `//bun/private/...` 나
`//bun/tests/...` 를 참조하지 않는다.
"""

load(":verify.bzl", "verify_toolchain")

verify_toolchain(name = "verify")
